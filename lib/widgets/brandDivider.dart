import 'package:flutter/material.dart';

class BrandDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(height: 1, color: Colors.black45, thickness: 1);
  }
}
